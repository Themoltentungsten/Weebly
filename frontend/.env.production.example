# Copy to .env.production locally only if you build manually.
# On Netlify, set VITE_API_URL in Site settings → Environment variables (Build).

# Public URL of your Render (or other) API — no trailing slash
VITE_API_URL=https://your-service.onrender.com
