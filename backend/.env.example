DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/weebly
JWT_SECRET=change_this_to_a_long_random_string
PORT=5000
# Optional — mood bot; catalog mode works without these
GEMINI_API_KEY=
GROQ_API_KEY=
# Comma-separated extra CORS origins (e.g. https://yourdomain.com). *.netlify.app allowed by default.
# CORS_ORIGINS=
# CORS_ALLOW_ALL=1
