{
  "name": "theweebly",
  "private": true,
  "description": "Weebly anime site — API + Vite frontend",
  "scripts": {
    "start": "concurrently -n api,web -c cyan,magenta \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "start:site": "npm start",
    "stop": "node scripts/stop-site.js",
    "stop:site": "npm run stop",
    "db:shell": "node scripts/db-shell.js",
    "db:web": "node scripts/db-web.js"
  },
  "dependencies": {
    "@vendia/serverless-express": "^4.12.6"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "dotenv": "^16.4.7"
  }
}
